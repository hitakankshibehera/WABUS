# Database Configuration (PostgreSQL DDL schema deliverable)
DATABASE_URL=postgresql://wabus_admin:secure_password@localhost:5432/wabus_production

# Server Session & OTP Secret
SESSION_SECRET=wabus_enterprise_secure_cookie_session_secret_2026

# Transactional Email Provider Configuration (SMTP / Resend / SendGrid / SES)
EMAIL_HOST=smtp.mailtrap.io
EMAIL_PORT=587
EMAIL_USER=your_smtp_username
EMAIL_PASSWORD=your_smtp_password
EMAIL_FROM="wABus By WonderlightAdvent" <auth@wabus.in>

# OTP Authentication Policy
OTP_EXPIRY_MINUTES=5
OTP_LENGTH=6
RATE_LIMIT_MAX_ATTEMPTS=5
